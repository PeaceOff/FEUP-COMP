module program1{
	data=[100]; //vector of 100 integers
	mx = 2; //attribute mx
	mn = 3; //attribute mn
	
	function det(d[],a){
		i=0;
		M=d.size-1;
		
		while(i<M){
			a=d[i];i=i+1;b=-d[i];
			mx= library1.max(a,b);
			mn= library1.min(a,b);
		}
		
		if(b<c){
			z=1;
		}else{
			k=0;
		}
	}
	
	function main(){
		det = 2;
		det(data);
		io.println("max:",mx);
		io.println("min:",mn);
	}
}