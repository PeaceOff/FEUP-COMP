module program1{
	banana = 1;
	data=[banana]; //vector of 100 integers
	mx = 2; //attribute mx
	mn = 3; //attribute mn
	
	function c = det(d[],a){
		i=0;
		M=d.size-1;
		a = 1 + a;
		while(i<M.size){
			a=d[i];i=i+1;b=-d[i];
			mx= library1.max(a,b);
			mn= library1.min(a,b);
		}
		while( i > funcaoXPTO()){
		}
		while(M.size <= 2){
		}
		while(M[1]>=M.size){
		}
		while(i.size==M.size){
		}
		while(i.size!=M.size){
		}
	}
	
	function main(){
		det = mx + det(data,mx);
		
		io.println("max:",mx);
		io.println("min:",mn);
	}
}