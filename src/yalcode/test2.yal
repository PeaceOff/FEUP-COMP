module program1{
	
	data=[100]; //vector of 100 integers
	mx; //attribute mx
	mn; //attribute mn
	
	function det(d[]){
		i=0;
		M=d.size-1;
		while(i<M.size){
			a=d[i];i=i+1;b=-d[i];
			mx= library1.max(a,b);
			mn= library1.min(a,b);
		}
		
		foo(d,i,M);
		foo();
	}
	
	function foo(a[],b,c){
	}
	
	function main(){
		det(potato,data);
		io.println("max:",mx);
		io.println("min:",mn);
	}
}